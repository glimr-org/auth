name = "glimr_auth"
version = "1.0.0"

description = "Auth layer for the Glimr web framework"
licences = ["MIT"]
repository = { type = "github", user = "glimr-org", repo = "auth" }

[dependencies]
# glimr = ">= 0.9.0 and < 0.10.0"
glimr = {path = "../framework"}
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
argus = ">= 1.0.0 and < 2.0.0"
simplifile = ">= 2.3.2 and < 3.0.0"
shellout = ">= 1.8.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
